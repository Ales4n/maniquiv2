{% comment %} MANIQUI: Validación social — 3 reseñas Google + bloque estadística — 2026-05-16 {% endcomment %}

{%- assign google_url = section.settings.google_url | default: '#' -%}

<section class="mtesti" id="maniquiTestimonios">
  <div class="mtesti__inner maniqui-container">

    <header class="mtesti__header">
      {%- if section.settings.eyebrow != blank -%}
        <p class="maniqui-eyebrow mtesti__eyebrow">{{ section.settings.eyebrow }}</p>
        <span class="maniqui-sep-line mtesti__sep"></span>
      {%- endif -%}
      <h2 class="mtesti__headline">
        {{- section.settings.headline -}}{% if section.settings.headline_em != blank %} <em class="mtesti__headline-em">{{ section.settings.headline_em }}</em>{% endif %}{% if section.settings.headline_post != blank %} {{ section.settings.headline_post }}{% endif %}
      </h2>
      {%- if section.settings.subheadline != blank -%}
        <p class="mtesti__sub">{{ section.settings.subheadline }}</p>
      {%- endif -%}
    </header>

    <div class="mtesti__grid">

      {%- for block in section.blocks -%}
        {%- if block.type == 'testimonio' -%}
          <article class="mtesti__card" {{ block.shopify_attributes }}>

            <div class="mtesti__stars" role="img" aria-label="5 de 5 estrellas">
              <span aria-hidden="true">★★★★★</span>
            </div>

            <div class="mtesti__quote-wrap">
              <span class="mtesti__quote-mark" aria-hidden="true">&ldquo;</span>
              <blockquote class="mtesti__quote">
                <p>{{ block.settings.quote }}</p>
              </blockquote>
            </div>

            <footer class="mtesti__attribution">
              <p class="mtesti__author">{{ block.settings.attribution }}</p>
              {%- if block.settings.service != blank -%}
                <p class="mtesti__service">{{ block.settings.service }}</p>
              {%- endif -%}
              <p class="mtesti__verified">Reseña verificada en Google</p>
            </footer>

          </article>
        {%- endif -%}
      {%- endfor -%}

      <aside class="mtesti__stats">
        <svg class="mtesti__google-g" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
          <path d="M12.545 10.239v3.821h5.445c-.712 2.315-2.647 3.972-5.445 3.972-3.332 0-6.033-2.701-6.033-6.032s2.701-6.032 6.033-6.032c1.498 0 2.866.549 3.921 1.453l2.814-2.814C17.503 2.988 15.139 2 12.545 2 7.021 2 2.543 6.477 2.543 12s4.478 10 10.002 10c8.396 0 10.249-7.85 9.426-11.748L12.545 10.239z" fill="currentColor"/>
        </svg>

        <p class="mtesti__rating-number">{{ section.settings.rating }}</p>

        <div class="mtesti__stars mtesti__stars--lg" role="img" aria-label="4,9 de 5 estrellas">
          <span aria-hidden="true">★★★★★</span>
        </div>

        <p class="mtesti__review-count">
          Basado en {{ section.settings.review_count }} reseñas verificadas
        </p>

        <a
          href="{{ google_url }}"
          class="mtesti__google-cta maniqui-btn"
          {% unless section.settings.google_url == blank %}target="_blank" rel="noopener"{% endunless %}
        >{{ section.settings.cta_label }}</a>
      </aside>

    </div>

  </div>
</section>

{% schema %}
{
  "name": "Validación social",
  "tag": "div",
  "class": "section-maniqui-testimonios",
  "settings": [
    {
      "type": "header",
      "content": "Encabezado"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Validación"
    },
    {
      "type": "text",
      "id": "headline",
      "label": "Titular",
      "default": "79 clientas."
    },
    {
      "type": "text",
      "id": "headline_em",
      "label": "Titular énfasis (sage cursiva)",
      "default": "4,9 estrellas."
    },
    {
      "type": "text",
      "id": "headline_post",
      "label": "Titular cierre",
      "default": "Y subiendo."
    },
    {
      "type": "textarea",
      "id": "subheadline",
      "label": "Subtítulo",
      "default": "Estas son algunas de las reseñas que recibimos en Google. Todas verificadas, todas reales."
    },
    {
      "type": "header",
      "content": "Bloque de estadística"
    },
    {
      "type": "text",
      "id": "rating",
      "label": "Puntuación media",
      "default": "4,9"
    },
    {
      "type": "text",
      "id": "review_count",
      "label": "Número de reseñas",
      "default": "79"
    },
    {
      "type": "text",
      "id": "google_url",
      "label": "URL perfil Google Business",
      "info": "Dejar vacío hasta tener la URL definitiva"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "Texto botón Google",
      "default": "Leer todas en Google →"
    }
  ],
  "blocks": [
    {
      "type": "testimonio",
      "name": "Reseña",
      "settings": [
        {
          "type": "textarea",
          "id": "quote",
          "label": "Cita (texto de la reseña)",
          "default": "[Testimonio pendiente de añadir]"
        },
        {
          "type": "text",
          "id": "attribution",
          "label": "Nombre / inicial",
          "default": "M.G."
        },
        {
          "type": "text",
          "id": "service",
          "label": "Servicio + plazo (MAYÚSCULAS)",
          "default": "BAJO DE PANTALÓN · 48H"
        }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [
    {
      "name": "Validación social",
      "settings": {
        "eyebrow": "Validación",
        "headline": "79 clientas.",
        "headline_em": "4,9 estrellas.",
        "headline_post": "Y subiendo.",
        "subheadline": "Estas son algunas de las reseñas que recibimos en Google. Todas verificadas, todas reales.",
        "rating": "4,9",
        "review_count": "79",
        "cta_label": "Leer todas en Google →"
      },
      "blocks": [
        { "type": "testimonio", "settings": { "quote": "[Testimonio pendiente de añadir]", "attribution": "M.G.", "service": "BAJO DE PANTALÓN · 48H" } },
        { "type": "testimonio", "settings": { "quote": "[Testimonio pendiente de añadir]", "attribution": "L.P.", "service": "AJUSTE DE HOMBROS · 3 DÍAS" } },
        { "type": "testimonio", "settings": { "quote": "[Testimonio pendiente de añadir]", "attribution": "A.F.", "service": "AJUSTE DE CINTURA · 48H" } }
      ]
    }
  ]
}
{% endschema %}

{% stylesheet %}
/* ─── Validación social ─── */
.section-maniqui-testimonios .mtesti {
  background: var(--cream);
  padding: 120px 0;
}

.section-maniqui-testimonios .mtesti__inner {
  display: flex;
  flex-direction: column;
  gap: 72px;
}

/* Header centrado */
.section-maniqui-testimonios .mtesti__header {
  text-align: center;
  max-width: 680px;
  margin: 0 auto;
}

.section-maniqui-testimonios .mtesti__eyebrow {
  margin-bottom: 16px;
}

.section-maniqui-testimonios .mtesti__sep {
  margin: 0 auto 24px;
}

.section-maniqui-testimonios .mtesti__headline {
  font-family: var(--font-display);
  font-weight: 300;
  font-size: clamp(36px, 4.2vw, 52px);
  line-height: 1.12;
  letter-spacing: -0.01em;
  color: var(--warm-dark);
  margin-bottom: 20px;
}

.section-maniqui-testimonios .mtesti__headline-em {
  font-style: italic;
  color: var(--sage);
}

.section-maniqui-testimonios .mtesti__sub {
  font-size: 15px;
  color: var(--text-muted);
  line-height: 1.65;
  max-width: 540px;
  margin: 0 auto;
}

/* Grid 4 columnas */
.section-maniqui-testimonios .mtesti__grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr) 1fr;
  gap: 24px;
  align-items: start;
}

/* Card reseña */
.section-maniqui-testimonios .mtesti__card {
  background: var(--cream-warm);
  border-top: 2px solid var(--sage);
  padding: 28px 24px;
  display: flex;
  flex-direction: column;
  min-height: 320px;
}

/* Estrellas */
.section-maniqui-testimonios .mtesti__stars {
  color: var(--sage);
  font-size: 15px;
  letter-spacing: 2px;
  margin-bottom: 20px;
  display: block;
}

/* Comillas decorativas + cita */
.section-maniqui-testimonios .mtesti__quote-wrap {
  position: relative;
  flex: 1;
  margin-bottom: 24px;
  padding-top: 8px;
}

.section-maniqui-testimonios .mtesti__quote-mark {
  position: absolute;
  top: -14px;
  left: -6px;
  font-family: var(--font-display);
  font-style: italic;
  font-size: 72px;
  line-height: 1;
  color: var(--sage);
  opacity: 0.22;
  pointer-events: none;
  user-select: none;
  z-index: 0;
}

.section-maniqui-testimonios .mtesti__quote {
  position: relative;
  z-index: 1;
  font-family: var(--font-display);
  font-style: italic;
  font-size: 18px;
  font-weight: 300;
  line-height: 1.55;
  color: var(--warm-dark);
}

/* Atribución */
.section-maniqui-testimonios .mtesti__attribution {
  margin-top: auto;
  padding-top: 20px;
  border-top: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.section-maniqui-testimonios .mtesti__author {
  font-family: var(--font-body);
  font-size: 14px;
  font-weight: 500;
  color: var(--warm-dark);
}

.section-maniqui-testimonios .mtesti__service {
  font-family: var(--font-body);
  font-size: 10px;
  font-weight: 600;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--sage);
}

.section-maniqui-testimonios .mtesti__verified {
  font-family: var(--font-body);
  font-size: 11px;
  color: var(--text-muted);
  margin-top: 2px;
}

/* Bloque estadística */
.section-maniqui-testimonios .mtesti__stats {
  background: var(--cream-warm);
  border: 1px solid var(--sage-line);
  padding: 32px 24px 36px;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

.section-maniqui-testimonios .mtesti__google-g {
  width: 20px;
  height: 20px;
  color: var(--text-muted);
  margin-bottom: 28px;
  flex-shrink: 0;
}

.section-maniqui-testimonios .mtesti__rating-number {
  font-family: var(--font-display);
  font-weight: 300;
  font-size: clamp(72px, 7vw, 96px);
  line-height: 1;
  color: var(--warm-dark);
  margin-bottom: 12px;
  letter-spacing: -0.02em;
}

.section-maniqui-testimonios .mtesti__stars--lg {
  font-size: 20px;
  letter-spacing: 3px;
  margin-bottom: 16px;
}

.section-maniqui-testimonios .mtesti__review-count {
  font-family: var(--font-body);
  font-size: 12px;
  color: var(--text-muted);
  line-height: 1.6;
  margin-bottom: 28px;
}

.section-maniqui-testimonios .mtesti__google-cta {
  background: transparent;
  color: var(--warm-dark);
  border-color: var(--sage-line);
  border-radius: 999px;
  padding: 10px 20px;
  font-size: 13px;
  min-height: auto;
}

.section-maniqui-testimonios .mtesti__google-cta:hover {
  border-color: var(--sage);
  color: var(--sage-dark);
  transform: translateY(-2px);
}

/* ─── Tablet: 2×2 ─── */
@media (max-width: 1024px) {
  .section-maniqui-testimonios .mtesti {
    padding: 80px 0;
  }
  .section-maniqui-testimonios .mtesti__grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }
}

/* ─── Mobile: stack ─── */
@media (max-width: 600px) {
  .section-maniqui-testimonios .mtesti {
    padding: 64px 0;
  }
  .section-maniqui-testimonios .mtesti__inner {
    gap: 48px;
  }
  .section-maniqui-testimonios .mtesti__grid {
    grid-template-columns: 1fr;
    gap: 16px;
  }
  .section-maniqui-testimonios .mtesti__card {
    min-height: auto;
  }
  .section-maniqui-testimonios .mtesti__stats {
    margin-top: 8px;
  }
}
{% endstylesheet %}
